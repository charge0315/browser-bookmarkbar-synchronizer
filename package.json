{
  "name": "browser-bookmarkbar-syncronizer",
  "version": "1.0.0",
  "description": "PCにインストールされている主要なブラウザ間で、ブックマークバーの内容をシームレスに同期・整理するためのブラウザベース・ツールです。",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
